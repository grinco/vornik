# Chunk event time

Chunks carry the time the content HAPPENED, distinct from when it was stored. Every temporal filter previously used created_at, including the window the memory-search tool exposes to the model.
