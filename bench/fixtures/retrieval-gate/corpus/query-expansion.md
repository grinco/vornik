# Query expansion

A short query is expanded before retrieval so that a two-word question can still match a paragraph phrased differently. Expansion runs before fusion and never replaces the original query text.
