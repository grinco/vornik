# Reciprocal rank fusion tie-breaking

When two candidates receive the same fused score, the order between them must be total and stable. Arbitrary tie-breaking made two identical runs rank differently while the retrieved document set stayed the same, so document-level metrics reported the runs as identical.
