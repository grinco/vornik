{
  "mcpServers": {
    "vornik": {
      "type": "http",
      "url": "${VORNIK_URL:-http://localhost:8080}/api/v1/mcp/companion",
      "headers": {
        "Authorization": "Bearer ${VORNIK_COMPANION_TOKEN}"
      }
    }
  }
}
