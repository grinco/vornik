{
  "name": "codex-companion",
  "version": "0.13.0+codex.20260804",
  "description": "Vornik async-offload companion for Codex. Delegate reviews, audits, validations, research, summarization, and project RAG memory work to a vornik daemon over MCP. 0.12.0: mirrors the claude-code 0.14.0 operator lifecycle triad — `configure-vornik` and `troubleshoot-vornik` join `report-problem`, carrying the same config-resolution hazards (fallback chain, silently skipped VORNIK_CONFIGS_DIR, cwd trap, restart-vs-reload) and the same symptom-routed diagnostic ladder. Copies are identical to the claude-code originals except that they address Codex. 0.13.0: mirrors the claude-code 0.15.0 `whoami` MCP tool — since Codex has no SessionStart hook announcing the detected repo scope (unlike Claude Code), the delegate skill now points Codex at `mcp__vornik__whoami` to check project_id / default_repo_scope / effective_repo_scope on demand, useful when the operator runs several Codex sessions a day against different projects/repos.",
  "author": {
    "name": "vornik"
  },
  "homepage": "https://vornik.io",
  "repository": "https://github.com/grinco/vornik",
  "license": "Apache-2.0",
  "keywords": [
    "vornik",
    "codex",
    "async",
    "delegation",
    "code-review",
    "agents",
    "mcp"
  ],
  "skills": "./skills/",
  "interface": {
    "displayName": "Vornik Companion for Codex",
    "shortDescription": "Delegate async agent work from Codex to vornik.",
    "longDescription": "Connects Codex to the vornik companion MCP endpoint so Codex can queue long-running reviews, audits, validation, research, summarization, and RAG memory operations without doing all work inline. 0.9.0 adds a `knowledge` skill teaching Codex the daemon-owned knowledge-skill store: propose reusable procedures (draft → operator-approved → served to swarm roles + all clients), search/apply approved skills, and approve with an admin key. Distinct from SWARM-SKILL.md capability skills and from RAG memory. 0.10.0 adds a `report-problem` skill teaching Codex how to help a user file an anonymized Vornik problem report (bug, crash, or INSTALL failure) as a prefilled github.com/grinco/vornik issue via the deterministic `vornikctl report` CLI (with `--offline`/`--dry-run`), guarded by review-before-submit and zero-auth (the user files under their own GitHub identity; nothing auto-posted). 0.11.0 corrects the delegate skill's file guidance for the T-8f69 staging fix (2026-07-25): the daemon now derives skip_auto_extract from a workflow's require_input_artifacts, so Codex no longer needs to set it when attaching inputArtifacts to companion-architectural-review / companion-doc-review / companion-rag-ingest. The prior text scoped that advice to companion-rag-ingest alone, so an artifact-bearing review delegated from Codex was auto-extracted at upload time — which suppressed raw-file staging and left roles without the document_* MCP tools unable to read the attachment at all.",
    "developerName": "vornik",
    "category": "Development",
    "capabilities": [
      "mcp",
      "skills",
      "async-delegation",
      "project-memory"
    ],
    "defaultPrompt": "Use the vornik companion MCP tools when work is large, async-friendly, or should be preserved in project memory. Recall before delegating research or review work. ALWAYS pass repo_scope on every recall/remember/recent_memory/delegate/memory_correct call: resolve it once per session from `git config --get remote.origin.url`, normalized to a host/path token (strip trailing .git, scheme, and any leading user@; replace the first ':' with '/') — e.g. github.com/grinco/vornik. Derive it from the remote; never hand-guess or lowercase it. Omitting repo_scope makes deposits land NULL-scoped and pollutes other repos' recalls."
  },
  "mcpServers": "./.mcp.json"
}
