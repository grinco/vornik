# Customer-facing top-level vornikctl commands published to docs.vornik.io.
# Deny-by-default: only groups listed here (and all their subcommands) appear
# in the generated CLI reference (docs/public/reference/vornikctl.md). Internal
# and admin commands (admin, blackbox, operator, scraper, cpc, eval, …) are
# intentionally absent. Add a line to publish a command group; remove to hide.
#
# NOTE on bench vs eval: `bench` is published because it is a tool a customer runs
# against their OWN deployment — it scores retrieval quality on datasets they supply
# or public ones it fetches. `eval` stays absent because it judges TASK outcomes
# against internal expectations. They are siblings in shape, not in audience.
init
project
swarm
workflow
task
execution
tail
key
config
doctor
memory
bench
instinct
knowledge
control-plane
models
mcp
skill
# package — the extension-package lifecycle (install/list/uninstall). Published
# on the same reasoning as `skill`: it is the verb an operator runs to accept an
# artifact somebody handed them, so hiding it would leave the artifact format
# documented and the only way to install it undocumented.
package
retention
backup
restore
reminders
version
